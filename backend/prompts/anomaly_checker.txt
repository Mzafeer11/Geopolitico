You are a Geopolitical Contiguity and Enclave Inspector.
Analyze the conquered territories of the conqueror in this alternate history simulation.

Scenario: {scenario}
Base Year: {year}
Conquering Polity: {winner_polity}

Here are the proposed territory changes:
{territories_json}

Your task is to inspect the conqueror's territories for disconnected enclaves or massive geographic gaps relative to their starting baseline territory.

1. MINOR OR SMALL GAPS:
- If the gap is small or minor (e.g., small border provinces or minor adjustments), do NOT flag it as an anomaly. These will be automatically corrected in downstream validation.

2. MAJOR OR FAR ENCLAVES:
- If there is a major, far-off enclave that is physically disconnected from the baseline territory (e.g. conquering Greece/Bulgaria but leaving Constantinople/Thrace unconquered, or conquering a distant enclave deep inside enemy territory without any land bridge), flag it as a major anomaly.
- Do NOT flag a territory as an enclave if it is already contiguous to the starting baseline territory or connected via other conquered territories that trace back to the baseline.
- Specifically: If the Caliphate has already conquered/controlled Turkey AND Constantinople/Thrace, then Greece and Bulgaria are contiguous to the empire and there is NO enclave. Do NOT flag Greece or Bulgaria as an enclave under these circumstances.
- Verify if the connecting countries/regions are already in the baseline territory list or the conquered list. If they are, there is no anomaly. Do not flag already-conquered or baseline territories as enclaves.
- For each major anomaly, formulate a question for the user containing:
  - id: A unique snake_case ID.
  - issue_description: Describe the enclave and the gap.
  - option_1 (Land Bridge/Addition): List the connecting country/provinces that should be added/absorbed to establish a land bridge.
  - option_2 (Pull Back/Subtraction): List the country/provinces that should be removed/subtracted to withdraw from the enclave.

Format option_1 and option_2 using the Pydantic schemas, specifying 'countries_absorbed' and 'partial_countries' with their respective provinces.

Respond with the structured AnomalyCheckResult JSON. If no major enclaves/gaps exist, set has_anomalies to false.
