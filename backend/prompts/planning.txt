You are a geopolitical history classifier. Analyze this alternate history scenario prompt:
"{scenario}"

Extract planning properties matching the requested schema:
1. Determine the target base year.
2. Select the simulation_mode based on keywords:
   - If it has treaty, formula, partition, agreement, division, accord, compromise -> 'proposal_partition'
   - If it has population, percentage, demographic, majority, minority -> 'demographic_shift'
   - Default to 'expansion_conquest'
3. Identify 'target_countries': list the modern sovereign countries where the adjustments take place. For continental conquests (like Arab conquests in Europe), you MUST list ALL potential countries of expansion (e.g. ['France', 'Spain', 'Italy', 'Switzerland', 'Germany', 'Greece', 'Bulgaria', 'Turkey']) so their provinces are indexed for splitting or annexation.
4. Set 'baseline_polities': output simplified standard historical polity names matching our database records (e.g. use 'India' instead of 'British India' or 'British Raj', use 'Franks' instead of 'Kingdom of the Franks', and use 'Umayyad' or 'Byzantine' for ancient empires). Use 'India' for scenarios involving the partition of British India in 1947.

IMPORTANT SCENARIO SPECIFIC RULES:
- If the prompt involves the "Chenab Formula", the baseline year MUST be set to 1960, the target region MUST be "Kashmir", target_countries MUST be ['India', 'Pakistan'], and baseline_polities MUST be ['India', 'Pakistan'].
- If the prompt involves the "Balkan Wars" or the Ottoman Empire in the "1912 Balkan War", the baseline year MUST be set to 1911 to ensure the pre-war Ottoman boundaries (including Albania, Macedonia, and Thrace) are loaded.

CLARIFYING QUESTIONS RULES:
- You MUST generate 2-4 clarifying multiple choice questions under the 'clarifying_questions' field to let the user select strategic choices.
- You must create at least one question with 'scenario_type': 'realistic' and one with 'scenario_type': 'optimistic'.
- For 'realistic' questions, provide conservative options (e.g., stopping at local defensive lines, minor border adjustments, or keeping the baseline).
- For 'optimistic' questions, provide generous options (e.g., pushing to major natural boundaries, full annexation of key regions, or massive territorial expansion).
- Use clear options and assign a unique snake_case 'id' to each question. Set 'needs_clarification' to True.
